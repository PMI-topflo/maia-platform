---
name: session_2026_06_13_phasec_vendor_selfservice
description: "2026-06-13 — Phase C PR2 (#371 merged) + vendor self-service ACH/W-9 (#372 open)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0f966d35-5727-4b77-8261-7f0eced7619a
---

**2026-06-13 session.** Two PRs.

**#371 (MERGED) — Phase C PR2 (estimate award).** In `finalizeEstimateApproval` (lib/estimate-approval-pdf.ts): C4 award notice now checks the winning vendor's CINC compliance and auto-requests **missing/expired** docs (ACH/W-9/COI/license) via a tokenized upload link; C6 sends not-selected notices to vendors who actually quoted; estimate request set to `closed` so the follow-up cron stops chasing. Idempotent via `estimate_request_vendors.outcome` + `outcome_notified_at` (migration `20260612_estimate_award_outcome.sql`, applied by owner). Fires **only at award when something is missing/expired** — NOT per invoice/WO (explicit owner rule).

**#372 (OPEN, awaiting merge) — vendor self-service ACH + W-9.** Two new tabs on `/vendor/upload/[token]` (Banking (ACH), Tax (W-9)):
- **Checks CINC first** (getCincVendorDetail). If on file → confirm-unchanged card: ACH shows **bank name (live lookup from routing #), full routing, account ••••last4, type**; W-9 shows name + TIN ••••last4. Else → entry form.
- Entry → **review screen** (account/TIN masked) → **full name + title** + responsibility certification (ACH authorization; verbatim IRS W-9 Part-II perjury language) → submit.
- Submit generates a signed PDF (ACH auth / Substitute W-9), files it on the WO classified `ach`/`w9`; staff apply to CINC via the EXISTING one-click **"→ CINC"** push (`updateVendorRecord` {Routing,Account,AccountType} / {TaxID,CheckName}). **Staff-confirm, not auto = bank-change fraud checkpoint.**
- **Security posture:** full account/routing/TIN live ONLY inside the generated PDF in storage; DB keeps last-4 + authorization record. Full values re-read server-side only at the push.
- **Deep-link-only:** tabs appear ONLY when the award email deep-links `?need=ach,w9` (derived from actually-missing docs); plain link = estimate/invoice/photos only.
- New: `lib/bank-routing.ts` (ABA checksum + **routingnumbers.info** name lookup, cached, graceful fallback), `lib/vendor-ach-authorization.ts`, `lib/vendor-w9-record.ts`, `app/api/vendor/upload/[token]/ach` + `/w9`, `AchSection.tsx`, `W9Section.tsx`. i18n en/es + English fallback; legal cert text English. NO migration.

⚠️ **POST-DEPLOY VERIFY (#372):** one real round-trip — submit an ACH and a W-9 through the portal, then click **"→ CINC"** on each — to confirm MAIA's AI re-read of the GENERATED PDFs extracts routing/account + TIN cleanly (the staff push `extractVendorDocument(mask:false)` reads the PDF text). This is the one integration point not verifiable locally.

**LATER 2026-06-13:** #372 MERGED. #373 MERGED — **Vendor Compliance audit page** (`/admin/vendor-compliance` + Control Panel card): vendors on active WOs, live CINC RAG (ACH/W9/COI/license), their files (lazy signed-URL links), and "Request missing docs" → editable templated email (deep-links `?need=ach,w9`) → send + WO note. Helper `lib/vendor-compliance-overview.ts`; APIs `/api/admin/vendor-compliance/{files,request}`. **#374 OPEN — Compliance Hub Phase 1** (`/admin/audit` rebuilt): Upload zone (existing MAIA Document Inbox moved in) + association picker → full association doc set via existing `ComplianceMatrix` (present/expired/missing + expiry + 📎 View link to filed doc; Sunbiz/insurance/DBPR/tax folded in) + "Units" tab keeps the old v_unit_compliance table. Added source_path to `/api/admin/compliance` GET + new `/api/admin/compliance/file` signed redirect + 📎 link in ComplianceMatrix. **#375 OPEN — Compliance Hub Phase 2** (unit/owner docs): classifier (`lib/document-classifier.ts`) now decides association-vs-unit scope + reads owner/unit identifier; intake matches it to an owner (`lib/owner-match.ts` — exact account# then name+unit) → files compliance_records scope='unit'. Document Inbox card gained Association/Unit badge + owner picker. Hub: picking an assoc lists every owner with on-file/missing unit-doc counts + expand (status/expiry/📎). New `/api/admin/compliance/units`. **Also in #375 (added after Lakeview feedback): MULTI-POLICY PACKET SPLITTING** — classifier returns ALL coverages in a bundled PDF (ACORD packet = GL/Property/Umbrella) each with page ranges; intake splits the PDF per policy (`lib/pdf-split.ts` pdf-lib) → one review row + one file per coverage (falls back to full file if range missing). **+ DOCUMENT PREVIEW** — each inbox review card has 👁 Preview (inline iframe via GET on the intake row → signed-URL redirect) + Open↗, so staff see the doc before filing (can't tell by filename). ⚠️ **Migration `20260613_document_intake_unit.sql` (document_intake_unit) MUST be applied BEFORE deploy — the intake insert writes the new columns or upload breaks.** POST-DEPLOY VERIFY: upload a real Lakeview ACORD packet → should split into separate GL/Property/Umbrella cards, each previewable, each filing to its own coverage+PDF. **REFINEMENTS (Lakeview round 2, in #375):** (1) **Append-to-previous** — over-split pieces store source_storage_path+page_start+page_end (added to the migration); "⤢ Append to previous" re-splits the union range from the original packet into the prev row, drops the extra. (2) **Crime recall** — prompt now lists ACORD section→item map (Crime/Fidelity→insurance.fidelity) + multi-page PDFs go straight to Sonnet for recall. (3) **Address matching** — associations passed to classifier with principal_address/city/state/zip/match_aliases; matches by ADDRESS when name absent (associations table HAS these cols). Splitter is page-range based (AI page boundaries can be imperfect → that's why append-to-previous exists). ⚠️ **SQUASH-STRAND happened again:** #375 merged at the split/preview state; the round-2 refinements commit pushed to that same branch AFTER merge got stranded → recovered as **#376** (cherry-picked onto merged main). Lesson reinforced: once a PR may be merged, STOP pushing to its branch — open a new branch. #376 migration = NEW `20260613_document_intake_split.sql` (document_intake_split: source_storage_path/page_start/page_end) — the base document_intake_unit shipped with #375. **#371–#376 ALL MERGED.** **#377 OPEN — unit-doc owner/unit resolver** (no migration): real Lakeview HO-6/lease/CoU uploads came back with NO association + NO owner/unit match even though unit# was on the page. Fix: `resolveOwnerForDocument` (lib/owner-match.ts) does a GLOBAL owners-table lookup keyed on unit number (near-unique per assoc) + owner name + property street address (+ exact account# as top signal); when MAIA didn't name the association the matched owner record supplies BOTH assoc + unit; matches owner even when the doc lists TENANTS (keys on unit#). Classifier unit_seen now captures owner/landlord name (preferred over tenant) + unit# + property address. ⚠️ ADDRESS-matching for unit docs must use the OWNER/UNIT record (owners table), NOT the association's principal_address (that's the mgmt mailing addr). **#377 MERGED. #378 OPEN — catalog expansion** (lib/compliance-taxonomy.ts, pure data, no migration): UNIT +4 (HO-4, Unit Manager Info, LLC/Trust Docs, Usage Type) + relabels; ASSOCIATION +3 categories (Licenses & Permits, Meetings, Risk Management) + Milestone recertification/building_condition. Existing assoc cats already cover SIRS/Milestone/Fire/Vendor/BoardCerts/Contracts/Governing/Sunbiz/Insurance/DBPR/Tax/Audit. **NEXT (not built, agreed):** (1) **Occupancy status** per-unit field (Owner-occupied/Leased/Vacant) → makes audit required-docs-AWARE (leased→Lease+Tenant+HO-4; owner-occ→HO-6; vacant→neither); first Q in owner self-service. (2) 🍒 **GOOGLE DRIVE BULK IMPORTER** — share parent folder with the SERVICE ACCOUNT email (reuse lib/drive-invoice-mirror.ts googleapis client, scope drive), paste link → recursive scan subfolders → use FOLDER NAMES as assoc+type hints → import each via existing classify/split/preview/review pipeline → import-all + FLAG-EXPIRED (don't auto-skip). (3) Owner self-service email: audit expired/missing per unit → per-owner link → upload missing + answer occupancy. Build order: #378 catalog → Drive importer → audit+owner-email. **#378 MERGED. #379 OPEN — 🍒 GOOGLE DRIVE BULK IMPORTER** (no migration): `lib/drive-import.ts` reuses invoice-mirror Drive client (`getDrive` exported from drive-invoice-mirror.ts); recursive folder walk (PDFs/images + subfolder breadcrumb) + download. Extracted shared `lib/document-intake-ingest.ts` (classify/split/owner-resolve/insert) used by BOTH browser upload + Drive import. `classifyDocument` gained `contextHint` (folder breadcrumb biases ambiguous classification). New APIs `/api/admin/documents/drive/scan` + `/drive/import`; `DriveImport.tsx` panel atop the inbox (also shows in Compliance Hub upload tab). Expired docs get red "Expired" badge in review card. ⚠️ **shareTargetEmail() = GOOGLE_DRIVE_IMPERSONATE (billing@) when set, NOT raw SA — folder must be shared with the IMPERSONATED user** (DWD client acts AS that user). Decisions: share-with-SA(impersonated) + import-all+flag-expired. POST-DEPLOY VERIFY: share a folder w/ that email → Scan → Import (needs prod Drive creds; GOOGLE_SERVICE_ACCOUNT_JSON empty locally). NEXT remaining: occupancy-status field + owner self-service audit-email. **#379 MERGED (Drive importer LIVE, works — scanned 53 files).** **#380 OPEN — fix: Drive scan only descended ONE branch** because sibling subfolders were Drive SHORTCUTS (common layout: "Unit Docs" links to each unit folder vs nesting). Fix: listFolderFilesRecursive resolves shortcutDetails (follow folder shortcuts, import file shortcuts by real id) + seen-set + pageSize 1000 + cap 2000 + returns foldersScanned ("N files across M folders" shown in panel). VERIFY post-deploy: re-scan Unit Docs → should report all unit subfolders. **CINC WEBAXIS DOCUMENT LIBRARY (ContentMan) is NOT accessible via the management API** — /documentIds is a separate accounting store (type=Report, negative FileIds); WebAxis /account/d/<id> ids don't resolve via /document and require a WebAxis session (return error-PDF unauth). So bulk-import CINC docs = NOT possible via API; use Drive importer. CINC has its own per-folder "Enable AI to read files (WebAxis/Cinc Manager)" toggles — ask CINC if those expose an API. **#381 OPEN — OWNER SELF-SERVICE PORTAL** (`/owner/compliance/[token]`, public token-gated): owner confirms occupancy (Owner-occupied/Leased/Vacant) → tailored missing-docs list → upload (runs through shared ingest → staff review queue). `lib/owner-portal-token.ts`, `lib/unit-required-docs.ts` (occupancy-aware: leased adds lease+tenant+HO-4+occupancy; base = ownership/contact/emergency/HO-6/rules_ack), `unit_occupancy` table (migration `20260614_unit_occupancy.sql` — apply before deploy; occupancy SAVE needs it, page reads fine without). Verified live (Darius Delva·Crystal Hills V·Unit 2). ⚠️ NEXT/LAST PIECE = the **audit cron** that finds units with expired/missing docs, mints these per-owner links (`signOwnerComplianceToken`), and emails owners — the trigger that drives traffic to the portal. Also still pending: occupancy field surfaced on the staff /admin/audit unit view. **#381 EXPANDED — full owner→tenant loop + association-aware insurance:** owner portal (leased) captures tenant contact (unit_tenant_contacts table, migration `20260614_unit_tenant_contacts.sql`); owner uploads notify staff (COMPLIANCE_NOTIFY_EMAIL). Staff "✉ Request docs from tenant" button in Compliance Hub unit view → `/api/admin/compliance/request-tenant` emails tenant. **Tenant portal `/renter/compliance/[token]`** (public, not in middleware matcher — /renter not /tenant) → confirm contact + upload HO-4/etc → shared ingest → staff review. **INSURANCE NOW ASSOCIATION-TYPE-AWARE** (was HO-6-for-all, WRONG): `lib/unit-required-docs.ts` keys off associations.association_type — condo/coop→HO-6, hoa/master_hoa→HO-3, commercial_condo→CPP/BOP, vacant→vacant policy; tenant: residential→HO-4, commercial→CGL+COI. Catalog gained ho3/cgl/commercial_property/vacant_policy. Commercial tenant portal shows additional-insured note (COI names Association+Mgmt Co+Landlord). association_type values: condo/commercial_condo/hoa/master_hoa/coop. Two migrations in #381 (unit_occupancy + unit_tenant_contacts). LAST PIECE = audit cron (find expired/missing → auto-email owners the link + reminders). ⚠️ CONFIRMED: Fabio's insurance occupancy→type table is correct (Condo→HO-6, Res Tenant→HO-4, HOA→HO-3, Commercial Tenant→CGL+COI, Commercial Owner→CPP/BOP, Vacant→Vacant Property Policy). **#378-#381 ALL MERGED.** ⚠️⚠️ **SQUASH-STRAND HAPPENED A 3RD TIME on #381** — pet/access/usage tenant fix + Maia branding + Association↔Unit toggle were pushed AFTER #381 merged → stranded. Recovered via cherry-pick into **#382 OPEN**. #382 also adds shared `components/PortalFormHeader.tsx` (Maia logo + "Compliance Module · ⚠ Action needed" chip) on ALL 3 public portals (vendor upload now branded too; owner; tenant). HARD LESSON RE-CONFIRMED: stop pushing to a PR branch once it may be merged; open a fresh branch for follow-ups. **AUDIT CRON BUILT — #382 + #383 OPEN.** #382 = shared PortalFormHeader (Maia logo + "Compliance Module·Action needed" chip on all 3 portals) + recovered strands. #383 = **owner compliance audit cron** (`lib/compliance-owner-audit.ts` + `/api/cron/owner-compliance-audit` + vercel.json weekly Mon 13:00 UTC + migration `owner_compliance_requests`): scans units for missing required docs → emails owner their /owner/compliance link; cadence 14d, max 4 reminders, marks resolved, capped 50/run, scoped per assoc. SAFE GATING: cron sends only if `OWNER_AUDIT_ENABLED=1`; staff dry-run by default, `?send=1&assoc=` for manual send. Verified dry-run CHV (8 owners, correct missing incl HO-6 condo). ROLLOUT: merge+migration → staff dry-run an assoc → ?send=1 that assoc → set OWNER_AUDIT_ENABLED=1 for auto reminders. **COMPLIANCE ARC COMPLETE** (vendor compliance, association+unit hub, Drive importer, owner+tenant self-service loop, association-aware insurance, audit cron). Pending owner-side: apply migrations (unit_occupancy, unit_tenant_contacts, owner_compliance_requests); merge #382+#383+#384; ask CINC re WebAxis doc-library API. **#384 OPEN — unit-docs dedupe**: `/api/admin/compliance/units` was listing one row PER OWNER (co-owners + duplicate developer/null-account rows repeated the same physical unit). Now groups by unit_number (fallback account_number), one row per unit, unit_ref prefers real account, co-owner names combined. Verified all 25 assocs (GVH 45→24). ⚠️ DATA-QUALITY FLAG: 6 associations have NO unit_number on owner rows → **BHB, ESSI, LCLUB, LFA, PVV, VPREC** (owner-side fix: import unit numbers; until then those list by account#). **ALL MERGED through #384** (no open PRs). Entire compliance arc shipped: #371-#384. REMAINING OWNER-SIDE (not code): apply 3 migrations (unit_occupancy, unit_tenant_contacts, owner_compliance_requests) in Supabase; to turn on auto owner-audit reminders set env OWNER_AUDIT_ENABLED=1 (until then weekly cron is inert; staff can dry-run/?send=1 per assoc); import unit_number for BHB/ESSI/LCLUB/LFA/PVV/VPREC; ask CINC re WebAxis doc-library API. Rollout audit: dry-run an assoc → ?send=1 that assoc → enable env. Migrations to apply (in #381, merged): unit_occupancy, unit_tenant_contacts. ⚠️ DEV PREVIEW RSC CACHE got stale repeatedly on server-component page edits — fix: preview_stop + rm -rf .next/cache + restart. ⚠️ Local-dev `/admin` dashboard was very slow + RangeError when hitting PROD Supabase over internet — environmental, not the vendor-count change (prod renders fine).

Branch discipline held: #372 branched/rebased off main AFTER #371 merged. See [[session_2026_06_11_recon_estimates]] for prior context. Still pending from before: Phase C PR3 (C5 agenda link → service_visit on assoc calendar), Service Issues PR2/3, recon monthly-export page, monthly-report enrichment.
