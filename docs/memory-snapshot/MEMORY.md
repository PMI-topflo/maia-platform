- ⏰ COMMUNICATION PREFERENCE: ALWAYS show times in **Eastern Time (ET)**, never UTC. User is in Florida (EDT = UTC−4 in summer, EST = UTC−5 in winter). Convert any DB/UTC timestamps to ET when reporting to the user.
- ⚙️ MAIA RELIABILITY HARDENING SESSION (2026-06-06/07) — incident fully resolved, maia@ LIVE. ROOT OUTAGE was an AUTH mismatch (Vercel `GMAIL_PUBSUB_SECRET` drifted vs Pub/Sub `?token=maia2026pmi` → webhook 401'd ALL pushes → zero delivery for ~8h; fixed by aligning the secret + redeploy). Shipped **#291–#303**: kill switch + cursor-BEFORE-processing (#291), global Claude circuit breaker (#292, armed), drop-the-15-cap (#293), ack-429 + deactivate-dead-token-staff (#294), **self-healing Gmail 429 cooldown** (#295, `gmail_cooldown_until` on maia_watch_state + staff_gmail_accounts), prompt caching on freeform+chat (#296), **passive health panel + cooldown-aware Diagnose/Sync** (#297 — buttons no longer re-trip the rate limit), resilient selects (#298), **invoice dedup by STABLE FILENAME not volatile gmail_attachment_id** (#299, the 88×-dup fix + `attachment_filename` migration), Pushed→**Archived** tab rename (#300), **skip email-signature/logo images + dedupe quoted attachments** (#301+#302), **resync skips already-seen via email_logs** (#302 — stops re-dup + re-ack on resume), **PMI-as-vendor on PMI-issued RVP invoices** (#303). Cleaned up live: invoice drafts 545→14, work-order photos 7223→2, swept ~7221 orphan storage objects. ⚠️ LESSONS: (1) **Gmail attachmentId is VOLATILE** (changes every messages.get) — never use as a dedup key; filename is stable. (2) **squash-merge strands commits pushed AFTER the merge** (the #301 logo-dedupe follow-up stranded; re-applied in #302) — push ALL commits before merging. (3) DON'T probe a throttled mailbox (each call resets Google's penalty). FOLLOW-UPS (owner-side, not built): existing RVP drafts in queue need manual vendor pick (read under old prompt); set up "PMI Top Florida Properties" as a CINC vendor for the associations so RVP invoices auto-match; review/push the 14 pending invoices; optional end-to-end test (forward 1 invoice to maia@).
- 🚦 GMAIL maia@ SUSTAINED THROTTLE (2026-06-06 night): After the 520k-call runaway, Google put a MULTI-HOUR rate-limit penalty on the maia@pmitop.com mailbox (per-account; staff inboxes unaffected — separate Google quotas). Symptom: "User-rate limit exceeded / RESOURCE_EXHAUSTED" with a rolling ~15-min `Retry after` that EXTENDS on EVERY Gmail call (incl. setup-watch/Diagnose/Sync). A 23-min quiet window did NOT clear it. KEY LESSONS: (1) DO NOT probe a throttled mailbox — each call resets Google's penalty timer; park a LONG `gmail_cooldown_until` in maia_watch_state and walk away. (2) It clears on Google's DAILY quota reset (~3 AM ET / midnight PT). (3) Invoice intake WAS maia@-email-ONLY — a manual PDF upload path was added 2026-06-17 (#386, `+ Add invoice` on /admin/invoices), so invoices no longer fully block when maia@ is down. (4) Root outage that night was actually an AUTH mismatch: Vercel GMAIL_PUBSUB_SECRET drifted vs Pub/Sub's `?token=maia2026pmi` → webhook 401'd ALL pushes → zero delivery; fixed by aligning the secret + redeploy. CLI `vercel env add` couldn't set the value via stdin (always wrote empty) — set it in the dashboard. To recover maia@: null/keep-current the cursor, set a long cooldown past the daily reset, stop touching it.
- [⚠️ MAIA runaway API-spend incident + safety controls](maia_runaway_incident.md) — 2026-06-06: Gmail webhook reprocess-loop burned ~520k Haiku calls/24h. Cursor-advanced-after-processing → Pub/Sub redelivered same batch forever. FIXED #291 (cursor-before-processing + cap 15 + MAIA_WEBHOOK_DISABLED kill). Backstop #292 (global Claude circuit breaker: MAIA_AI_DISABLED + 250/5min Postgres cap, needs migration to arm). Emergency levers: Pub/Sub Push→Pull, MAIA_WEBHOOK_DISABLED=1, MAIA_AI_DISABLED=1.
- [MAIA — CINC vs RENTVINE scope split](maia_cinc_vs_rentvine_scope.md) — CINC = HOA common areas only; RENTVINE = inside-unit residential maintenance. Must not mix in UI or AI replies.
- [Vendor compliance + CINC vendor-write](vendor_compliance_cinc_write.md) — 2026-06-04 session. SHIPPED #276–#279: vendor-doc Claude extraction + Apply-to-CINC ACH/W-9 push + compliance pre-check + Daily News 5AM-ET fix + email deep-link fix. NEXT (not started): COI validation (fuzzy name+address vs PMI + assoc property addr → flag/block/auto-correct) → COI/license push UI → vendor-compliance audit panel+page → Paola procurement-in-WO. ⚠️ PRs squash-merge → push all commits before merging or they strand.
- [CINC API — Swagger + in-repo reference](cinc_api_reference.md) — Endpoint map lives in CINC_API.md at the repo root; Swagger at integration.cincsys.io. Update the in-repo doc as new endpoints are discovered.
- [WPM Accounting — residential accounting partner](wpm_accounting_residential.md) — karizza@wpmaccounting.com handles RENTVINE-side accounting; future RentVine integration touchpoint.
- [MAIA master backlog](next_session_priorities.md) — NOTE: as of 2026-06-03 the canonical living backlog is in-repo at `docs/ROADMAP.md` + `docs/SESSION-HANDOFF.md` (read those first). 2026-06-03 shipped: PR #262 (invoice Tier-1: GL auto-select, auto-association, expense-GL on Pushed) + #263 (Tier-2: scheduled-pay-date upcoming payments, debt/escrow guard, funds-check tuning). NEXT: background-check verify (decision 4) → Staff Daily News (see [[staff_daily_news]]) → per-association app rules → owner self-service. This file below is older context. TOP: 2026-05-31 vendor/add-on/recurring work — merge #237/#238 + apply migrations + deploy Gmail add-on + env (CINC_SYNC_ENABLED, GOOGLE_SERVICE_ACCOUNT_JSON); 3 NEW requests (control-panel Recurring-WO flag card; non-recurring estimates→board report; non-recurring weekly office chase); Recurring 3b (buildable) + 3c (blocked on CINC WO-create verify). BELOW: A–I carry-forward — A invoice, B intake UX, C MAIA freeform, D webhook crash, E recon, F intake/QB, G CINC migration, **H application workflow (12)**, **I doc compliance (14)**. I3/I4/I7/I8 already shipped. Next priority: 3 new requests → A2 status sync → I5 vendor compliance → H1 applicant status → C1 MAIA Teaching → I13 doc AI retrieval.
- [MAIA project location](project_location.md) — Canonical repo now at ~/maia-platform (moved out of iCloud); sessions open in home dir, cd into ~/maia-platform. Stale copies in Documents/GitHub + Downloads — ignore.
- [Owner self-service decisions](owner_self_service_decisions.md) — ledger: 1× OTP then request by email/WhatsApp/text; payments: CINC WebAxis/check/ACH (Stripe is application-fee ONLY); app-forms gap = add per-association rules acknowledgment inside existing /apply flow.
- [Staff Daily News feature](staff_daily_news.md) — branded daily Mon–Fri email, per-staff ticket/WO opened/resolved/open/late counts + "suggest MAIA improvement" link → admin accept/done/delete board. Tickets+WO both in `tickets` table, attributed ONLY by assignee_email. Open decisions before building (unassigned tickets, "late" def, send time, etc.).
- [Supabase migration workflow](migration_workflow.md) — User applies migrations by hand (Supabase SQL editor) and it keeps breaking. Migrations MUST be idempotent; paste literal SQL not file paths; register in lib/migration-status.ts.
- [Invoice process rework + bug root-causes](invoice_process_rework.md) — PR #267: auto-save on confirm, Transfer-to-Push, PDF block-on-fail (normalize at push), Drive-mirror-at-transfer, double-pay HARD-block (same vendor+amount+assoc, Karen-only override), re-attach button. Drive mirror failing for ALL = likely GOOGLE_SERVICE_ACCOUNT_JSON unset in prod / folder not shared w/ SA. CINC had NO double-pay (16271 Miriam, 16272 Michele no-PDF).
- [Invoice GL dropdown rules](invoice_gl_dropdown_rules.md) — Filter: expense-range (5–9) AND has-activity AND NOT-reserve. Reserves are a funding-source decision, not an expense category.
- [Invoice payment source](invoice_payment_source.md) — Use CINC's `PayFromBankAccountID` field (native, not a memo). Bank accounts come from `/banking/bankBalances`. `Reserve` boolean is broken — detect kind by description text.
- [Funds-check income model](funds_check_income_model.md) — Cash-flow forecast must model assessment income cadence (monthly vs quarterly) + the end-of-month/quarter low point, not just balance−bills. PR #361 (`lib/cash-flow-forecast.ts`) supersedes #360.
- [2026-06-13 — Phase C PR2 + vendor self-service ACH/W-9](session_2026_06_13_phasec_vendor_selfservice.md) — #371 merged (award + auto-request missing/expired docs + loser notices); #372 OPEN (vendor self-service ACH+W-9 → CINC, confirm-or-update, deep-link-only tabs). ⚠️ post-deploy verify: portal-submit ACH+W-9 then "→ CINC" round-trip.
- [2026-06-11 session — recon + estimates](session_2026_06_11_recon_estimates.md) — Shipped #349–#368 (invoice PDF, Phase C estimate-approval PR1, service-issues PR1, income-aware funds-check + cash-flow strip, reconciliation batch, CINC doc-preview, invoice search, Paola guide). PENDING: Phase C PR2/3, Service Issues PR2/3, recon monthly-export page, monthly-report enrichment, application process, background-check + Stripe pay, AI voice agent, Jonathan compliance pkg.
- [Multi-language — ALREADY BUILT](multi_language.md) — Resident-facing MAIA supports 6 languages EN/ES/PT/FR/HE/RU (app/page.tsx); vendor-crew side is a separate en/es/pt/ht/fr set. NOT a backlog item — don't call it missing.
- [Reconciliation workflow](reconciliation_workflow.md) — 2026-06-05. Recurring-estimate fixes (#283 skip transfers, #284 key off expense-side debit, #285 project EFT into real month + dedup) + #286 Jonathan "To Pay in CINC" box/Mark-Paid + Isabela/Jonathan daily recon tickets (NEEDS migration). Stacked PRs #283→#284→#285→#286, merge in order. CINC has NO mark-Paid API (Paid = CINC payment run). "CSB-Cash Operating-1950" in GK7 = GK7's own payments mislabeled with CHV's bank-acct name (CINC label bug; no money moved cross-assoc).
- [CINC cleanup revisit workflow](cinc_cleanup_revisit.md) — When user says "revise all chart of accounts, they are fixed" → re-run dump-association-gl.ts for all 25 assocs and check against docs/cinc-cleanup-2026-05-26.md.
- [Gmail sidebar add-on](gmail_addon.md) — guided ticket creation + visibility inside Gmail; backend /api/addon/* + token via /admin/addon + source in gmail-addon/; staff reply capture relies on maia@ staying CC'd on threads.
- [Vendor upload portal](gmail_addon.md) — tokenized login-free /vendor/upload/[token] for vendors to upload estimates/invoices/photos to a WO (staff-triggered link); chosen over parsing vendor inbound emails. See gmail_addon.md.
- [Vendor/recurring backlog + handoff](vendor_recurring_backlog.md) — open items as of 2026-05-31: merge #237/#238 + apply migrations + deploy add-on; new requests (Recurring-WO control-panel flag, non-recurring estimates→board report, non-recurring weekly office chase); Phase 3b/3c; smaller follow-ups.
- [2026-06-17 — compliance outreach + invoice/WO + full vendor onboarding](session_2026_06_17_compliance_vendor_onboarding.md) — shipped #385–#394 (ALL merged, migrations applied): Compliance Outreach, manual Add-invoice, WO payment lifecycle + association switcher, board-approval popup, Operating Manuals, cash-flow transfer-exclusion fix, ACH/W-9 invoice gate (cc Paola), full vendor onboarding (createVendor VERIFIED + dedupe + portal + ACH-confirm). AUDIT: voice IVR + application/leasing are ALREADY BUILT; top gaps = Applycheck results webhook (MISSING), owner payments/ledger, per-assoc app rules. TODO: set OWNER_AUDIT_ENABLED=1; delete CINC test vendor 2121.
- ⭐ [Implementation roadmap 2026-06-18](implementation_roadmap_2026_06.md) — MASTER prioritized plan. **MAIA is STAFF-ONLY today** (internal routines, not customers) → customer-facing items deferred. TOP now: (1) WO workflow — New-WO form ✅ DONE (#396); **estimate-comparison board report WITH IMAGES = next**; (2) Application package — **Applycheck results webhook still MISSING** + board report + package assembly. Deferred: voice agent, owner payments. Then compliance Phase 2.
- ⭐ [Session 2026-06-18/19 — Personas, vendor linking, portals, docs→MAIA](session_2026_06_18_personas_portals_docs.md) — shipped #399–#405 (ALL merged): Personas hub + per-person Messages, vendor↔association MAIA-local linking (CINC link is READ-ONLY), HEIC→JPEG via heic-convert, hub type/statute fix, resident-portal 25→1 shared component + identity hero, portal docs moved OFF Google Drive INTO MAIA. ⚠ PENDING when resuming (laptop 06-19→~06-29): (1) apply `20260618_association_vendor_links.sql` in Supabase; (2) UPLOAD each association's documents into MAIA (Drive removed → portals empty until uploaded; GVH not auto-migrated).
- [Voice agent plan (DEFERRED — customer-facing)](voice_plan.md) — decided: **Vapi** (Build plan) + Twilio + bring-your-own-Claude via stateless `/chat/completions` SSE shim + Deepgram STT + Cartesia/ElevenLabs TTS + pgvector retrieval; voice = latency "fast lane". REJECTED Twilio ConversationRelay & Retell (need persistent WS, Vercel can't host). SKIP Alexa/Siri/OAuth device-linking (conflicts with natural-voice goal; phone identity = caller-ID via existing buildCallerContext). Don't rebuild persona resolver / staff admin (exist). NOT started — MAIA staff-only.
